# Changelog · nanju-write-paper

本文件记录 skill 的所有重要变更。
版本遵循语义化版本（MAJOR.MINOR.PATCH）；每个版本对应一个 git tag。

## [1.1.1] — 2026-08-29

修复 SVG 对外分发时的回退字体溢出（README 配图在访客机器上出边界/压线）。

### 新增
- **`assets/svg-autofit.js`**：SVG 分发场景的字体自适应修正器——注入最宽回退字体（微软雅黑）模拟，检测画布越界/容器越界/文字互压，自动缩小超界文字字号（下限 9px），返回可写回的 XML。幂等（重跑 fixes=0）。
- **diagram-guide §5.1「SVG 分发场景的字体适配」**：文字预算按回退字体最宽值计算（拉丁/标点 +15%）；对外分发必跑 svg-autofit 并复检 fixes=0；残余越界 = 缩短文案，禁止再压字号；内嵌 PDF/HTML 的图不必跑。

### 修复
- README 5 张配图 SVG 全部通过微软雅黑最坏情况适配（panorama 26 处、skeleton 1 处、outputs 2 处收字）；README 徽章移至标题下方、首图之前。

## [1.1.0] — 2026-08-29

跨介质交付规范 + 演示形态硬约束 + 配图配额，全部来自《pi 解剖》实战与双向盲区盘查。

### 新增
- **画幅形态规则**（style-guide §6.8）：16:9=演示形态（每页必有视觉主体、纯文字页禁止、单页 ≤350 字、真图 ≥5/图式 ≥4）；A4/长图=文章形态；3:4=半图半文。
- **配图配额硬约束**（diagram-guide §1.5）：真图 ≥4（演示 ≥5）、图式 ≥3（演示 ≥4）、fig-N 对账（缺失或降级成表格 = P1）；时序图/信息图/架构图触发词。
- **跨介质交付十条例**（style-guide §7）：公众号图表 PNG 化、长图 890-1080px/≤5000px 限高、3:4 安全区、深色页打印墨耗提示、A4 @page+页码+break-inside、viewport/color-scheme/letterbox、随稿封面图+摘要、降级地图。
- **信息图模块**（theme.css `.statrow`/`.big`/`.k2`，含暗页变体）并登记进 style-guide。
- **表格模块** `.dtable`（非分级语义的紧凑数据表）。
- **实战样例**：`writing/2026-08-29-pi-agent/` 端到端全产物（7000 字终稿 + 20 页 16:9 演示稿）。

### 修复
- diagram-check.js 增加样式加载守卫（第 0 步校验 .page 画幅定宽），杜绝 CSS 404 导致的全页裸渲染假通过。
- diagram-check.js 增加 `[clipped-*]` 整页裁切检测与 `[lifelines-missing]` 生命线贯穿检测；越权 absolute 检查只扫直接子元素并豁免 .msg/.ring 内建定位。
- 时序图生命线 `grid-row: 2/99`（原 `-1` 在隐式网格下失效）；`.seq .msg` 补 `grid-column: 1/-1`。
- html2pdf.sh Windows 加固：绝对路径解析、独立 user-data-dir、PowerShell URL 编码、stderr 落盘、候选浏览器路径补全。
- 公众号模板去除微信会剥离的 `box-shadow`/`float`，右对齐注记降级为行内「· 分隔」。
- theme.css 密度令牌上调 + `.page` 弹性纵排 + `.codecard--dock`/`.panel--fill`/`.points--fill` 停靠填充工具类。

### 变更
- S4 审查从四角色扩为五角色：新增 R5 人味审读 10%（detect-only、独立上下文、14 条快检清单）；权重改为 30/20/25/15/10。
- 打回协议明确：每轮修完全部 P0/P1 点名条目；加权公式写死；3 轮不过升级用户裁决的两种出口。
- 闸门缺省规则：追问 1 次无回复按建议值继续并标注「未经用户确认」。
- S1 调研增加任务卡四要素、复杂度路由（2-4 路）、工具预算 ≤12 次/路、research-plan 先落盘再派工、核心结论 ≥2 独立来源。
- S3 增加节末反提纲 diff（防越写越水）；Claim 状态机 supported/weak/unsourced。
- G2 终稿归档时增量更新 `references/voice-profile.md` 风格基线。

## [1.0.0] — 2026-08-29

首个验证版。

### 新增
- Phase A 写作流水线：G0 简报 → S1 调研 → S2 编排 → G1 大纲 → S3 写作 → S4 审查 → G2 终稿，全流程闸门与打回循环。
- Phase B 排版：南橘卡片设计系统（自 000-reference 8 张参考卡提取：深咖 #231e18 × 米杏 #f9f5ea × 赤陶橙 #cd642d，衬线+等宽双字体），三种格式（PDF/HTML/公众号）× 四种画幅（A4/16:9/3:4 卡片/手机长图）。
- 配图体系：手搭 HTML/CSS/SVG（禁自动布局），四类质量问题（出边界/压线/紊乱/重叠）的防治与机器检查。
- 活人感契约初版、多 Agent 名册与提示词骨架、样例库回灌飞轮。
- 端到端验证：《pi 解剖》五角色加权 8.20 分 PASS，20 页渲染验收通过。
